development:
  adapter: redis
  url: redis://localhost

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: rnds_web_sniffer_production
